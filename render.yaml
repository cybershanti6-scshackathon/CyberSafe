services:
  - type: web
    name: cybersure
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn msme_auditor.api.server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
